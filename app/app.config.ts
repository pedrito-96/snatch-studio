export default defineAppConfig({
  ui: {
    main: {
      // base: 'min-h-[calc(100vh-var(--ui-header-height))] overflow-hidden
      // '
      base: 'min-h-screen overflow-hidden'
    },
    colors: {
      primary: 'green',
      neutral: 'slate'
    }
  }
})
